//b2 to b10
#include <iostream> 
using namespace std;
//represent as array, take array and sizeof as parameter, returning int
//b2 to 10 requires division by 2. b10 is remainder. %
 int bin_to_int(int binary_digits[], int number_of_digits) {
    for b2[](i => 1; i <= 30; i++); //scan through each element of array
    b2[](&i % 2); //% symbol leaves remaining number as result
    number_of_digits(j => 1; j <= 30; j++); //counts number of digits
    sum = &j++;
    number_of_digits = sum;
 }

 int main() {}